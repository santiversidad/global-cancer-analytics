import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Oculta el indicador flotante de Next.js en desarrollo (la "N" de la esquina inferior izquierda)
  devIndicators: false,
};

export default nextConfig;
